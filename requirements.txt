# ENRG-AI — intelligence layer of the AXIS ecosystem
# (hybrid AI oracle, federated learning, market signals)
ollama
numpy>=1.24
httpx>=0.24
PyNaCl>=1.5          # Ed25519-signed federated contributions (agent/fed)

# dev / tests
pytest>=7.0
